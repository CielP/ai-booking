# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Frontend build output
frontend/dist/
frontend/dist-ssr/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# Docker — named volumes are managed by Docker, not stored locally
# (pg_data is a named volume in docker-compose.yml, not a bind mount)
pgdata/

# Claude Code — local settings are machine-specific
.claude/settings.local.json

# .claude (commands/skills) and openspec are intentionally tracked in version control
